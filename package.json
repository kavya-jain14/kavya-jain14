{
  "name": "kavya-profile",
  "private": true,
  "type": "module",
  "scripts": {
    "build:portrait": "python3 scripts/build_portrait_dotmatrix.py",
    "build:projects": "node scripts/build-project-cards.mjs",
    "build": "npm run build:portrait && npm run build:projects"
  },
  "engines": {
    "node": ">=20"
  }
}
